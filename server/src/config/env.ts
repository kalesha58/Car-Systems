// This file must be imported FIRST before any other modules
// It ensures environment variables are loaded before anything else runs
import dotenv from 'dotenv';

// Load .env file
dotenv.config();

export {};

