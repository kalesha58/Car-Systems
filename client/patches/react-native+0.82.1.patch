diff --git a/node_modules/react-native/scripts/cocoapods/helpers.rb b/node_modules/react-native/scripts/cocoapods/helpers.rb
index cdb1e14..b7370fb 100644
--- a/node_modules/react-native/scripts/cocoapods/helpers.rb
+++ b/node_modules/react-native/scripts/cocoapods/helpers.rb
@@ -3,6 +3,8 @@
 # This source code is licensed under the MIT license found in the
 # LICENSE file in the root directory of this source tree.
 
+require 'shellwords'
+
 # Helper object to wrap the invocation of sysctl
 # This makes it easier to mock the behaviour in tests
 class SysctlChecker
@@ -31,7 +33,7 @@ class Finder
     def self.find_codegen_file(path)
         js_files = '-name "Native*.js" -or -name "*NativeComponent.js"'
         ts_files = '-name "Native*.ts" -or -name "*NativeComponent.ts"'
-        return `find #{path} -type f \\( #{js_files} -or #{ts_files} \\)`.split("\n").sort()
+        return `find #{Shellwords.escape(path)} -type f \\( #{js_files} -or #{ts_files} \\)`.split("\n").sort()
     end
 end
 
diff --git a/node_modules/react-native/scripts/cocoapods/new_architecture.rb b/node_modules/react-native/scripts/cocoapods/new_architecture.rb
index 8c7508b..846f1f5 100644
--- a/node_modules/react-native/scripts/cocoapods/new_architecture.rb
+++ b/node_modules/react-native/scripts/cocoapods/new_architecture.rb
@@ -4,6 +4,7 @@
 # LICENSE file in the root directory of this source tree.
 
 require 'json'
+require 'shellwords'
 
 require_relative "./utils.rb"
 require_relative "./helpers.rb"
@@ -165,7 +166,7 @@ class NewArchitectureHelper
         excluded_info_plist = ["/Pods", "Tests", "metainternal", ".bundle", "build/", "DerivedData/", ".xcframework", ".framework", "watchkitapp", "today-extention"]
         projectPaths.each do |projectPath|
             projectFolderPath = File.dirname(projectPath)
-            infoPlistFiles = `find #{projectFolderPath} -name "Info.plist"`
+            infoPlistFiles = `find #{Shellwords.escape(projectFolderPath)} -name "Info.plist"`
             infoPlistFiles = infoPlistFiles.split("\n").map { |f| f.strip }
 
             infoPlistFiles.each do |infoPlistFile|
diff --git a/node_modules/react-native/scripts/cocoapods/utils.rb b/node_modules/react-native/scripts/cocoapods/utils.rb
index 4d48727..8c5cbd4 100644
--- a/node_modules/react-native/scripts/cocoapods/utils.rb
+++ b/node_modules/react-native/scripts/cocoapods/utils.rb
@@ -225,7 +225,7 @@ class ReactNativePodsUtils
         file_path = file_manager.join(relative_path, '.xcode.env')
 
         if !file_manager.exist?(file_path)
-            system("echo 'export NODE_BINARY=$(command -v node)' > #{file_path}")
+            system("echo 'export NODE_BINARY=$(command -v node)' > #{Shellwords.escape(file_path)}")
         end
 
         if !file_manager.exist?("#{file_path}.local")
@@ -234,7 +234,7 @@ class ReactNativePodsUtils
             # Using `node --print "process.argv[0]";` we are able to retrieve the actual path from which node is running.
             # see https://github.com/facebook/react-native/issues/43285 for more info. We've tweaked this slightly.
             node_binary = Shellwords.escape(`node --print "process.argv[0]"`.strip)
-            system("echo 'export NODE_BINARY=#{node_binary}' > #{file_path}.local")
+            system("echo 'export NODE_BINARY=#{node_binary}' > #{Shellwords.escape(file_path + '.local')}")
         end
     end
 
diff --git a/node_modules/react-native/scripts/react-native-xcode.sh b/node_modules/react-native/scripts/react-native-xcode.sh
index 1dcdf55..c98dc94 100755
--- a/node_modules/react-native/scripts/react-native-xcode.sh
+++ b/node_modules/react-native/scripts/react-native-xcode.sh
@@ -148,7 +148,9 @@ if [[ -n "$CONFIG_JSON" ]]; then
 elif [[ -n "$CONFIG_CMD" ]]; then
   EXTRA_ARGS+=("--config-cmd" "$CONFIG_CMD")
 else
-  EXTRA_ARGS+=("--config-cmd" "'$NODE_BINARY' $NODE_ARGS '$REACT_NATIVE_DIR/cli.js' config")
+  # Use shell-escaped paths so project directories with apostrophes (e.g. Baig's) do not break sh -c.
+  _RN_CLI_JS="$REACT_NATIVE_DIR/cli.js"
+  EXTRA_ARGS+=("--config-cmd" "$(printf '%q' "$NODE_BINARY")${NODE_ARGS:+ $NODE_ARGS} $(printf '%q' "$_RN_CLI_JS") config")
 fi
 
 # shellcheck disable=SC2086
